<h1>Blogs#index</h1>
<p>Find me in app/views/blogs/index.html.erb</p>
<p><%= notice %></p>

<table>
  <tr>
    <th>タイトル</th>
    <th>内容</th>
  </tr>

<% @blogs.each do |blog| %>
  <tr>
    <td><%= blog.title %></td>
    <td><%= blog.content %></td>
    <td><%= link_to '詳細を確認する', blog_path(blog.id) %></td>
    <td><%= link_to 'ブログを編集する', edit_blog_path(blog.id) %></td>
    <!-- #link_toメソッドはデフォルトでHTTPメソッドをgetで送信するため、
    destroyアクションへのリクエストはdeleteメソッドを指定して送信する必要があります。-->
    <td><%= link_to 'ブログを削除する', blog_path(blog.id), method: :delete %></td>
    
    
  </tr>
<% end %>
</table>

<br>

<%= link_to '新しくブログを投稿する', new_blog_path %>



    
